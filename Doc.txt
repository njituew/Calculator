doc для калькулятора

Button:
 bd=x -- рамки, толщиной в х
 
Entry:
 justify=RIGHT -- начало ввода справа

grid:
 window.grid_column/rowconfigure(x,minsize=a) -- устанавливаем минимальное значение размеров колонок и строк
 columnspan=x -- объединяем колонки
 stick='wens' -- растягиваем виджет на сторны света (west, east, ...)
 padx/y = x -- расстояние между виджетом по x и y


eval -- из '1+1' делает 2 0_0
a[:-1] -- a не доходя до последнего элемнта